#include <stdio.h>
#include <math.h>
int main(){
	 int so;
     printf("nhap so chan \n");
     scanf("%d",&so);
     if(so%2==0){
     	
     	printf("can bac 2 cua so %d la %lf",so,sqrt(so));
	 }
	 else{
	 	printf("xin vui long nhap lai \n");
	 }
}

